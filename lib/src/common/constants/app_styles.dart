import 'package:flutter/material.dart' show Colors, Color;

class AppStyles {
  static const double mainPadding = 20;
  static const double mainMargin = 20;
  static const double mainBorderRadius = 20;
  static const double pagePadding = 16;
  static Color backgroundColor = Colors.grey[50]!;
  static Color errorColor = Colors.red[300]!;
  static Color textGrey = Colors.grey[600]!;
  static Color fillGrey = Colors.grey[200]!;
  static const Color primaryColor = Color.fromARGB(255, 71, 61, 212);
  static const Color grayscaleOffWhite = Color(0xFFFCFCFC);
  static const Color disableText = Color(0xFF949698);
  static const Color disable = Color(0xFFD8DCDE);
}
